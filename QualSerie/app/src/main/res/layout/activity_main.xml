<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="caraoucoroa.studio.brunocasamassa.qualserie.MainActivity">

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/pouco"
        android:layout_marginBottom="27dp"
        android:id="@+id/rostoid"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="Muito"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/muitoid"
        tools:text="muito"
        android:layout_below="@+id/logo"
        android:layout_toEndOf="@+id/rostoid"
        android:layout_marginTop="23dp" />

    <SeekBar
        android:layout_height="wrap_content"
        android:id="@+id/seekBarid"
        android:layout_width="250dp"
        android:max="4"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/logo"
        android:id="@+id/logo"
        android:layout_below="@+id/poucosid"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="Pouco"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/muitoid"
        android:layout_alignStart="@+id/seekBarid"
        android:id="@+id/poucofd"
        tools:text="Pouco" />

</RelativeLayout>
