<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="opt2flow.com.br.magolandiaapp">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.MainActivity" />
        <activity
            android:name=".Controller.MenuActivity"
            android:label="@string/title_activity_menu"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Controller.BluetoothActivity" />
        <activity
            android:name=".Controller.NovoPersonagemActivity"
            android:label="Cadastrar Personagem" />
        <activity android:name=".Controller.BotoesActivity" />
        <activity
            android:name=".Controller.NovaExpressaoActivity"
            android:label="Expressões" />
        <activity android:name=".Controller.ConfigurarControleManualActivity" />
        <activity android:name=".Controller.ExpressoesActivity"></activity>
    </application>

</manifest>